buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 23
        compileSdkVersion = 35
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.8.22"
    }
    repositories {
        google()
        mavenCentral()
    }
   dependencies {
    classpath("com.android.tools.build:gradle:8.6.0")                 // Use an Android Gradle Plugin version compatible with your SDK
    classpath("com.facebook.react:react-native-gradle-plugin:0.74.2")  // Match your RN version
    classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22")    // Kotlin plugin version consistent with kotlinVersion ext
}
}
